move a repo to the local machine
git clone https://develop-eng.github.io/portal-10.git 

to see changes in repo:
git status 

stage all changed files: 
git add .

see difference between files:
git diff

commit changes to master :
git commit -m "adds files to branch" 

move the changed files to server :
git push
